{
  "domain": "frontend",
  "weekNumber": 2,
  "title": "Deep Dive: React Performance Optimization",
  "description": "Master React performance optimization techniques including memoization, code splitting, lazy loading, and bundle size optimization. Learn to identify performance bottlenecks and implement solutions for production-ready applications.",
  "publishedDate": "2026-01-20",
  "difficulty": "advanced",
  "estimatedTime": 210,
  "mainArticle": {
    "id": "main-article",
    "title": "The Complete Guide to React Performance Optimization",
    "description": "Comprehensive guide covering useMemo, useCallback, React.memo, code splitting, and profiling techniques",
    "type": "article",
    "difficulty": "advanced",
    "estimatedTime": 55,
    "content": {
      "markdown": "# The Complete Guide to React Performance Optimization\n\nPerformance is crucial for user experience. A slow React app loses users. This guide covers everything you need to build blazing-fast React applications.\n\n## Why Performance Matters\n\n- **User Experience**: 53% of users abandon sites that take >3 seconds to load\n- **SEO**: Google considers page speed in rankings\n- **Conversion**: Every 100ms delay reduces conversion by 7%\n- **Retention**: Fast apps keep users engaged\n\n## Understanding React's Rendering\n\n### How React Renders\n\n```javascript\n// React re-renders when:\n// 1. State changes\n// 2. Props change\n// 3. Parent component re-renders\n// 4. Context value changes\n\nfunction Parent() {\n  const [count, setCount] = useState(0);\n  \n  // Child re-renders even if it doesn't use count!\n  return (\n    <div>\n      <button onClick={() => setCount(c => c + 1)}>+</button>\n      <Child /> {/* Re-renders on every click */}\n    </div>\n  );\n}\n```\n\n### The Render Cycle\n\n```\n1. Trigger (state/prop change)\n2. Render (call component functions)\n3. Reconciliation (diff virtual DOM)\n4. Commit (update real DOM)\n```\n\n## React.memo - Prevent Unnecessary Re-renders\n\n`React.memo` is a higher-order component that memoizes the result.\n\n```javascript\n// Without memo - re-renders on every parent update\nfunction ExpensiveList({ items }) {\n  console.log('ExpensiveList rendered');\n  return (\n    <ul>\n      {items.map(item => (\n        <li key={item.id}>{item.name}</li>\n      ))}\n    </ul>\n  );\n}\n\n// With memo - only re-renders if props change\nconst MemoizedList = React.memo(function ExpensiveList({ items }) {\n  console.log('ExpensiveList rendered');\n  return (\n    <ul>\n      {items.map(item => (\n        <li key={item.id}>{item.name}</li>\n      ))}\n    </ul>\n  );\n});\n```\n\n### Custom Comparison Function\n\n```javascript\nconst MemoizedComponent = React.memo(\n  function MyComponent({ user, settings }) {\n    return <div>{user.name}</div>;\n  },\n  (prevProps, nextProps) => {\n    // Return true if props are equal (skip re-render)\n    return prevProps.user.id === nextProps.user.id;\n  }\n);\n```\n\n## useMemo - Memoize Expensive Calculations\n\n`useMemo` caches the result of expensive computations.\n\n```javascript\nfunction ProductList({ products, filter }) {\n  // BAD: Filters on every render\n  const filteredProducts = products.filter(p => \n    p.name.toLowerCase().includes(filter.toLowerCase())\n  );\n\n  // GOOD: Only recalculates when products or filter changes\n  const filteredProducts = useMemo(() => {\n    console.log('Filtering products...');\n    return products.filter(p => \n      p.name.toLowerCase().includes(filter.toLowerCase())\n    );\n  }, [products, filter]);\n\n  return (\n    <ul>\n      {filteredProducts.map(p => <li key={p.id}>{p.name}</li>)}\n    </ul>\n  );\n}\n```\n\n### When to Use useMemo\n\n✅ **Use when:**\n- Computation is genuinely expensive\n- Processing large arrays\n- Complex calculations\n- Creating objects passed to memoized children\n\n❌ **Don't use for:**\n- Simple operations\n- Premature optimization\n- Every single value\n\n```javascript\n// Unnecessary - simple operation\nconst doubled = useMemo(() => count * 2, [count]);\n\n// Worthwhile - expensive operation\nconst sortedData = useMemo(() => {\n  return [...data].sort((a, b) => {\n    // Complex sorting logic\n    return complexComparison(a, b);\n  });\n}, [data]);\n```\n\n## useCallback - Memoize Functions\n\n`useCallback` returns a memoized callback function.\n\n```javascript\nfunction Parent() {\n  const [count, setCount] = useState(0);\n  \n  // BAD: New function created every render\n  const handleClick = () => {\n    console.log('clicked');\n  };\n  \n  // GOOD: Same function reference unless dependencies change\n  const handleClick = useCallback(() => {\n    console.log('clicked');\n  }, []);\n  \n  // With dependencies\n  const handleIncrement = useCallback(() => {\n    setCount(c => c + 1);\n  }, []); // No deps needed with functional update\n  \n  return <MemoizedChild onClick={handleClick} />;\n}\n\nconst MemoizedChild = React.memo(function Child({ onClick }) {\n  console.log('Child rendered');\n  return <button onClick={onClick}>Click</button>;\n});\n```\n\n### Common Mistake\n\n```javascript\n// This breaks memoization!\nfunction Parent() {\n  const data = { id: 1, name: 'Test' }; // New object every render\n  \n  return <MemoizedChild data={data} />; // Always re-renders!\n}\n\n// Fix with useMemo\nfunction Parent() {\n  const data = useMemo(() => ({ id: 1, name: 'Test' }), []);\n  \n  return <MemoizedChild data={data} />; // Properly memoized\n}\n```\n\n## Code Splitting with React.lazy\n\nSplit your bundle to load code on demand.\n\n```javascript\nimport React, { Suspense, lazy } from 'react';\n\n// Instead of static import\n// import HeavyComponent from './HeavyComponent';\n\n// Dynamic import - loads only when needed\nconst HeavyComponent = lazy(() => import('./HeavyComponent'));\nconst Dashboard = lazy(() => import('./Dashboard'));\nconst Settings = lazy(() => import('./Settings'));\n\nfunction App() {\n  return (\n    <Suspense fallback={<LoadingSpinner />}>\n      <Routes>\n        <Route path=\"/\" element={<Home />} />\n        <Route path=\"/dashboard\" element={<Dashboard />} />\n        <Route path=\"/settings\" element={<Settings />} />\n      </Routes>\n    </Suspense>\n  );\n}\n```\n\n### Named Exports with lazy\n\n```javascript\n// For named exports, create an intermediate module\nconst MyComponent = lazy(() => \n  import('./MyModule').then(module => ({\n    default: module.MyComponent\n  }))\n);\n```\n\n## Virtualization for Long Lists\n\nRender only visible items using react-window or react-virtuoso.\n\n```javascript\nimport { FixedSizeList } from 'react-window';\n\nfunction VirtualizedList({ items }) {\n  const Row = ({ index, style }) => (\n    <div style={style}>\n      {items[index].name}\n    </div>\n  );\n\n  return (\n    <FixedSizeList\n      height={400}\n      width=\"100%\"\n      itemCount={items.length}\n      itemSize={35}\n    >\n      {Row}\n    </FixedSizeList>\n  );\n}\n\n// Without virtualization: 10,000 DOM nodes\n// With virtualization: ~20 DOM nodes (only visible)\n```\n\n## Debouncing and Throttling\n\nLimit how often expensive operations run.\n\n```javascript\nimport { useState, useCallback, useMemo } from 'react';\nimport debounce from 'lodash/debounce';\n\nfunction SearchInput({ onSearch }) {\n  const [value, setValue] = useState('');\n  \n  // Debounce the search - wait 300ms after last keystroke\n  const debouncedSearch = useMemo(\n    () => debounce((query) => onSearch(query), 300),\n    [onSearch]\n  );\n  \n  const handleChange = (e) => {\n    const newValue = e.target.value;\n    setValue(newValue);\n    debouncedSearch(newValue);\n  };\n  \n  return (\n    <input\n      value={value}\n      onChange={handleChange}\n      placeholder=\"Search...\"\n    />\n  );\n}\n```\n\n### Custom useDebounce Hook\n\n```javascript\nfunction useDebounce(value, delay) {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n  \n  useEffect(() => {\n    const timer = setTimeout(() => {\n      setDebouncedValue(value);\n    }, delay);\n    \n    return () => clearTimeout(timer);\n  }, [value, delay]);\n  \n  return debouncedValue;\n}\n\n// Usage\nfunction Search() {\n  const [query, setQuery] = useState('');\n  const debouncedQuery = useDebounce(query, 300);\n  \n  useEffect(() => {\n    if (debouncedQuery) {\n      fetchResults(debouncedQuery);\n    }\n  }, [debouncedQuery]);\n}\n```\n\n## Image Optimization\n\n### Lazy Loading Images\n\n```javascript\n// Native lazy loading\n<img src=\"large-image.jpg\" loading=\"lazy\" alt=\"Description\" />\n\n// With Intersection Observer for more control\nfunction LazyImage({ src, alt }) {\n  const [isLoaded, setIsLoaded] = useState(false);\n  const imgRef = useRef();\n  \n  useEffect(() => {\n    const observer = new IntersectionObserver(([entry]) => {\n      if (entry.isIntersecting) {\n        setIsLoaded(true);\n        observer.disconnect();\n      }\n    });\n    \n    if (imgRef.current) {\n      observer.observe(imgRef.current);\n    }\n    \n    return () => observer.disconnect();\n  }, []);\n  \n  return (\n    <img\n      ref={imgRef}\n      src={isLoaded ? src : placeholder}\n      alt={alt}\n    />\n  );\n}\n```\n\n### Next.js Image Component\n\n```javascript\nimport Image from 'next/image';\n\n// Automatically optimizes images\n<Image\n  src=\"/large-image.jpg\"\n  width={800}\n  height={600}\n  alt=\"Description\"\n  placeholder=\"blur\"\n  priority={false} // Lazy load by default\n/>\n```\n\n## React DevTools Profiler\n\n### Using the Profiler\n\n1. Open React DevTools\n2. Go to \"Profiler\" tab\n3. Click \"Record\"\n4. Interact with your app\n5. Click \"Stop\"\n6. Analyze the flame graph\n\n### Programmatic Profiling\n\n```javascript\nimport { Profiler } from 'react';\n\nfunction onRenderCallback(\n  id,           // Component name\n  phase,        // \"mount\" or \"update\"\n  actualDuration, // Time spent rendering\n  baseDuration,   // Estimated time without memoization\n  startTime,\n  commitTime\n) {\n  console.log(`${id} took ${actualDuration}ms`);\n}\n\nfunction App() {\n  return (\n    <Profiler id=\"App\" onRender={onRenderCallback}>\n      <MyComponent />\n    </Profiler>\n  );\n}\n```\n\n## Bundle Size Optimization\n\n### Analyze Bundle\n\n```bash\n# For Create React App\nnpm install --save-dev source-map-explorer\nnpx source-map-explorer build/static/js/*.js\n\n# For Next.js\nnpx @next/bundle-analyzer\n```\n\n### Tree Shaking\n\n```javascript\n// BAD: Imports entire library\nimport _ from 'lodash';\nconst result = _.debounce(fn, 300);\n\n// GOOD: Import only what you need\nimport debounce from 'lodash/debounce';\nconst result = debounce(fn, 300);\n```\n\n### Dynamic Imports for Heavy Libraries\n\n```javascript\n// Load heavy libraries on demand\nasync function generatePDF() {\n  const { jsPDF } = await import('jspdf');\n  const doc = new jsPDF();\n  // ...\n}\n```\n\n## State Management Performance\n\n### Avoid Unnecessary Context Re-renders\n\n```javascript\n// BAD: All consumers re-render on any state change\nconst AppContext = createContext();\n\nfunction Provider({ children }) {\n  const [user, setUser] = useState(null);\n  const [theme, setTheme] = useState('light');\n  \n  // New object every render!\n  const value = { user, setUser, theme, setTheme };\n  \n  return (\n    <AppContext.Provider value={value}>\n      {children}\n    </AppContext.Provider>\n  );\n}\n\n// GOOD: Split contexts by update frequency\nconst UserContext = createContext();\nconst ThemeContext = createContext();\n\n// Or memoize the value\nfunction Provider({ children }) {\n  const [user, setUser] = useState(null);\n  const [theme, setTheme] = useState('light');\n  \n  const value = useMemo(\n    () => ({ user, setUser, theme, setTheme }),\n    [user, theme]\n  );\n  \n  return (\n    <AppContext.Provider value={value}>\n      {children}\n    </AppContext.Provider>\n  );\n}\n```\n\n## Performance Checklist\n\n### Before Optimizing\n1. ✅ Measure first - don't optimize prematurely\n2. ✅ Use React DevTools Profiler\n3. ✅ Identify actual bottlenecks\n4. ✅ Set performance budgets\n\n### Quick Wins\n1. ✅ Use production builds\n2. ✅ Enable gzip/brotli compression\n3. ✅ Optimize images\n4. ✅ Use CDN for static assets\n5. ✅ Implement caching headers\n\n### React-Specific\n1. ✅ Use React.memo for expensive components\n2. ✅ Use useMemo for expensive calculations\n3. ✅ Use useCallback for stable function references\n4. ✅ Implement code splitting\n5. ✅ Virtualize long lists\n6. ✅ Debounce expensive operations\n\n### Common Mistakes\n\n❌ Premature optimization\n❌ Memoizing everything\n❌ Ignoring bundle size\n❌ Not measuring impact\n❌ Over-engineering state management\n\n## Conclusion\n\nPerformance optimization is about finding and fixing real bottlenecks, not applying every technique everywhere.\n\n**Key Takeaways:**\n1. Measure before optimizing\n2. Use React DevTools Profiler\n3. Apply memoization strategically\n4. Code split aggressively\n5. Virtualize long lists\n6. Monitor bundle size\n\n**Remember:** The fastest code is code that doesn't run. Only optimize what matters.\n\nHappy optimizing! 🚀"
    }
  },
  "resources": [
    {
      "id": "resource-1",
      "title": "React Profiler DevTools Tutorial",
      "description": "Learn to use React DevTools Profiler to identify performance bottlenecks",
      "url": "https://react.dev/learn/react-developer-tools",
      "type": "tutorial",
      "difficulty": "intermediate",
      "estimatedTime": 40
    },
    {
      "id": "resource-2",
      "title": "Web.dev Performance Guide",
      "description": "Google's comprehensive guide to web performance optimization",
      "url": "https://web.dev/performance/",
      "type": "article",
      "difficulty": "intermediate",
      "estimatedTime": 45
    },
    {
      "id": "resource-3",
      "title": "React-Window Documentation",
      "description": "Learn virtualization for rendering large lists efficiently",
      "url": "https://react-window.vercel.app/",
      "type": "tutorial",
      "difficulty": "intermediate",
      "estimatedTime": 35
    }
  ],
  "topics": ["React", "Performance", "Optimization", "Memoization", "Code Splitting", "Bundle Size"],
  "keywords": ["useMemo", "useCallback", "React.memo", "lazy loading", "code splitting", "profiler", "bundle optimization", "virtualization"]
}
